import{useEffect,useState}from'react';import{ShieldCheck}from'lucide-react';import{api}from'../lib/api';import{Badge,Empty,Spinner}from'../components/ui';
export default function Audit(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{api.get('/audit').then(r=>setItems(r.data)).catch(()=>{})},[]);return <div><div className="page-head"><div><span className="eyebrow">SEGURIDAD</span><h1>Auditoría</h1><p>Registro de acciones importantes realizadas en el sistema.</p></div></div>{!items.length?<Spinner/>:<div className="audit-list">{items.map(a=><div className="audit-row" key={a.id}><div className="audit-icon"><ShieldCheck size={17}/></div><div className="audit-main"><strong>{a.action} · {a.entity}</strong><span>{a.user?.name||'Sistema'} · {new Date(a.createdAt).toLocaleString('es-GT')}</span></div><Badge>{a.entityId?.slice(0,10)||'—'}</Badge></div>)}</div>}</div>}
