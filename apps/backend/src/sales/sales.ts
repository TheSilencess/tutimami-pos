import {BadRequestException,Body,Controller,Get,Param,Post,Query,UseGuards}from'@nestjs/common';import {IsArray,IsEnum,IsInt,IsNumber,IsOptional,IsString,Min,ValidateNested}from'class-validator';import {Type}from'class-transformer';import {PaymentMethod,InventoryMovementType,SaleStatus}from'@prisma/client';import {PrismaService}from'../common/prisma.service';import {AuthGuard}from'../common/auth.guard';import {Permission,CurrentUser}from'../common/auth.decorators';import {AuditService}from'../common/audit.service';
class ItemDto{@IsString()productId!:string;@IsInt()@Min(1)quantity!:number;@IsNumber()@Min(0)discount!:number}@classValidator()class PayDto{@IsEnum(PaymentMethod)method!:PaymentMethod;@IsNumber()@Min(0)amount!:number;@IsOptional()@IsString()reference?:string}function classValidator(){return (t:any)=>t}
class SaleDto{@IsString()customerId!:string;@IsArray()@ValidateNested({each:true})@Type(()=>ItemDto)items!:ItemDto[];@IsNumber()@Min(0)discount!:number;@ValidateNested()@Type(()=>PayDto)payment!:PayDto}
@Controller('sales')@UseGuards(AuthGuard)export class SalesController{constructor(private p:PrismaService,private audit:AuditService){}@Get()@Permission('sales.view')list(@Query('from')from?:string,@Query('to')to?:string){return this.p.sale.findMany({where:{createdAt:{...(from?{gte:new Date(from)}:{}),...(to?{lte:new Date(to+'T23:59:59.999Z')}: {})}},include:{customer:true,user:{select:{name:true}},items:true,payments:true},orderBy:{createdAt:'desc'},take:200})}@Get(':id')@Permission('sales.view')one(@Param('id')id:string){return this.p.sale.findUnique({where:{id},include:{customer:true,user:{select:{name:true}},items:{include:{product:true}},payments:true}})}@Post()@Permission('sales.create')async create(@Body()d:SaleDto,@CurrentUser()u:any){if(!d.items.length)throw new BadRequestException('La venta debe tener productos');return this.p.$transaction(async tx=>{const customer=await tx.customer.findUnique({where:{id:d.customerId}});if(!customer)throw new BadRequestException('Cliente no encontrado');let subtotal=0;const lines:any[]=[];for(const i of d.items){const p=await tx.product.findUnique({where:{id:i.productId}});if(!p||!p.active)throw new BadRequestException('Producto inválido');if(p.stock<i.quantity)throw new BadRequestException(`Stock insuficiente: ${p.name}`);const base=Number(p.salePrice)*i.quantity;const discount=Math.min(i.discount,base);const net=base-discount;const tax=0;const total=Number(net.toFixed(2));subtotal+=base;lines.push({p,i,discount,tax,total})}const discount=Number(d.discount||0);const taxable=Math.max(0,subtotal-discount);const tax=0;const total=Number(taxable.toFixed(2));if(d.payment.amount<total)throw new BadRequestException('El pago es menor al total');const sale=await tx.sale.create({data:{customerId:d.customerId,userId:u.id,subtotal,discount,tax,total,status:SaleStatus.PAID,items:{create:lines.map(x=>({productId:x.p.id,productName:x.p.name,quantity:x.i.quantity,unitPrice:Number(x.p.salePrice),discount:x.discount,tax:x.tax,total:x.total}))},payments:{create:{method:d.payment.method,amount:d.payment.amount,reference:d.payment.reference}}}});for(const x of lines){const prev=x.p.stock;const next=prev-x.i.quantity;await tx.product.update({where:{id:x.p.id},data:{stock:next}});await tx.inventoryMovement.create({data:{productId:x.p.id,userId:u.id,type:InventoryMovementType.SALE,quantity:-x.i.quantity,previousStock:prev,newStock:next,reason:`Venta ${sale.id}`}})}await tx.auditLog.create({data:{userId:u.id,action:'CREATE',entity:'Sale',entityId:sale.id,saleId:sale.id,newValues:{total}}});return sale})}@Post(':id/cancel')@Permission('sales.cancel')async cancel(@Param('id')id:string,@CurrentUser()u:any){return this.p.$transaction(async tx=>{const sale=await tx.sale.findUnique({where:{id},include:{items:true}});if(!sale||sale.status===SaleStatus.CANCELLED)throw new BadRequestException('Venta no válida');for(const i of sale.items){const p=await tx.product.findUnique({where:{id:i.productId}});if(!p)throw new BadRequestException(`Producto no encontrado: ${i.productId}`);const next=p.stock+i.quantity;await tx.product.update({where:{id:p.id},data:{stock:next}});await tx.inventoryMovement.create({data:{productId:p.id,userId:u.id,type:InventoryMovementType.RETURN,quantity:i.quantity,previousStock:p.stock,newStock:next,reason:`Cancelación ${sale.id}`}})}const out=await tx.sale.update({where:{id},data:{status:SaleStatus.CANCELLED}});await tx.auditLog.create({data:{userId:u.id,action:'CANCEL',entity:'Sale',entityId:id,saleId:id}});return out})}}
